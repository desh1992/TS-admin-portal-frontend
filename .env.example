# Base URL of the TalentShare backend API.
# The frontend calls <VITE_API_BASE_URL>/api/... and defaults to localhost
# when unset. On Heroku this is set via `heroku config:set`.
VITE_API_BASE_URL=http://localhost:3000
# Production: https://talent-share-admin-backend-4cfceb6f0d7f.herokuapp.com
